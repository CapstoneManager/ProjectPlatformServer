# Set to 'create' to init db, set to 'update' once db has been created
spring.jpa.hibernate.ddl-auto=update
#spring.datasource.url=jdbc:mysql://localhost:3306/capstone_db
#spring.datasource.username=root
#spring.datasource.password=

server.port = 8443

# Tell Spring Security (if used) to require requests over HTTPS
security.require-ssl=true
# The format used for the keystore 
server.ssl.key-store-type=PKCS12
# The path to the keystore containing the certificate
server.ssl.key-store=/etc/letsencrypt/live/cs401projects.tk/keystore.p12
# The password used to generate the certificate
server.ssl.key-store-password=jeffreymillerphd
# The alias mapped to the certificate
server.ssl.key-alias=tomcat

spring.datasource.url=jdbc:mysql://localhost:3306/db
spring.datasource.username=root
spring.datasource.password=jeffreymillerphd

#spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# ===============================
# SMTP EMAIL
# ===============================
spring.mail.host = smtp.gmail.com
spring.mail.username = csci401server@gmail.com
spring.mail.password = drowssap$$$
spring.mail.port = 587
spring.mail.properties.mail.smtp.auth = true
spring.mail.properties.mail.smtp.starttls.enable = true

spring.session.store-type=jdbc
